# Interactive Information Visualization Project - Group 2

- Install dependencies: `npm install`
- Run on localhost: `npm run serve`
- Regenerate the monthly delay bundles after changing the source CSVs: `npm run data:optimize`
